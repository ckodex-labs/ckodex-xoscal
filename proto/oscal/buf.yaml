version: v2
modules:
  - path: .
    name: buf.build/awslabs/oscal
lint:
  use:
    - STANDARD
  except:
    - FIELD_LOWER_SNAKE_CASE
    - PACKAGE_DIRECTORY_MATCH
breaking:
  use:
    - FILE
    - PACKAGE
deps:
  - buf.build/googleapis/googleapis
