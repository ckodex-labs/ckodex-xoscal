apiVersion: v1
kind: ConfigMap
metadata:
  name: spire-auth-config
  namespace: oscal
data:
  SPIRE_TRUST_DOMAIN: "spiffe://prod.example.com"
  SPIRE_OIDC_DISCOVERY_URL: "https://oidc-discovery.prod.example.com"
  SPIRE_AUDIENCE: "https://api.anthropic.com"
  SPIRE_AGENT_SOCKET: "/run/spire/sockets/agent.sock"
